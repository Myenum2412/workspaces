info:
  name: Forgot Password
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/api/auth/forgot-password"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "john@acme.com"
      }

settings:
  encodeUrl: true
  timeout: 15000
  followRedirects: true
  maxRedirects: 5
