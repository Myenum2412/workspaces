info:
  name: DB Proxy - Get Document
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/db"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
  body:
    type: json
    data: |-
      {
        "method": "getDocument",
        "collectionId": "user_profiles",
        "documentId": "document-id-here"
      }

settings:
  encodeUrl: true
  timeout: 15000
  followRedirects: true
  maxRedirects: 5
