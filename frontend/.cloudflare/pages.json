{
  "name": "workspaces-frontend",
  "build_command": "cd frontend && npm install --legacy-peer-deps && npm run build",
  "output_dir": "frontend/.next/standalone",
  "environment": {
    "production": {
      "NEXT_PUBLIC_BACKEND_URL": "YOUR_BACKEND_URL_HERE"
    }
  }
}
